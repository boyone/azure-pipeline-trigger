trigger:
  - main

variables:
- group: FOO

steps:
  - script: |
      echo 'first pipeline'
    displayName: First
  
  - script: |
      echo $(CANDIDATED_TAG)
    displayName: Echo Group Variables

  - bash: |
      "##vso[task.setvariable variable=CANDIDATED_TAG;isOutput=true]secondValue"
