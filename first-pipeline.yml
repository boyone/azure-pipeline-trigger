trigger:
  - main

variables:
- group: FOO

steps:
  - script: |
      echo 'first pipeline'
    displayName: First
  
  - script: |
      echo $(CANDIDATED_TAG)
    displayName: Echo Group Variables

  - script: |
      "##vso[task.setvariable variable=CANDIDATED_TAG;]secondValue"
